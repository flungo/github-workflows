{
  // markdownlint-cli2 config for this repo's docs. Starts from defaults; each
  // override below carries its justification. Repo-specific — regenerate rather
  // than copy when adopting elsewhere.
  "config": {
    // Line length off: tables, long links, and workflow snippets routinely
    // exceed any sensible column limit, and prose is not hard-wrapped.
    "MD013": false,
    // Allow repeated subsection headings under different parents (e.g. an ADR's
    // Context / Decision / Consequences recurring across records).
    "MD024": { "siblings_only": true },
    // Compact tables, one space each side of every pipe. Pinned rather than
    // left to MD060's ambiguous default ("consistent" infers per table, and
    // picks "aligned" where no row disambiguates). Same reasoning as MD013
    // above: a diff should be the size of the change. Under "aligned" cell
    // width is shared state — every cell is padded to its column's widest, so
    // one edit reflows the whole table and one long cell taxes every row.
    "MD060": { "style": "compact" }
  }
}
